Description:

    This generator will create an easy-to-use interface to allow trusted 
    users edit structured content. (Admin scaffold generator)

Examples:
    `./script/generate typus`

        creates needed files with `TypusUser` as the Typus user.

    `./script/generate typus -u User

        creates needed files with `User` as the Typus user.
