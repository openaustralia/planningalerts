Hi <%= @comment.name %>,

Local councillor <%= @comment.councillor.name %> replied to your message about the planning application “<%= @comment.application.description %>” at <%= @comment.application.address %>:

Local councillor <%= @comment.councillor.name %> replied:

<%= @reply.text %>

Best wishes,

PlanningAlerts
