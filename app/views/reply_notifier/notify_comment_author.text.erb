Hi <%= @comment.name %>,

Local councillor <%= @comment.councillor.name %> replied to your message
about the planning application “<%= @comment.application.description %>”
at <%= @comment.application.address %>:

Local councillor <%= @comment.councillor.name %> replied:

<%= @reply.text %>

=========================================================================

View and share this reply on PlanningAlerts:
<%= application_url(@comment.application,
                    protocol: "https",
                    anchor: "reply#{@reply.id}",
                    utm_campaign: "view-reply",
                    utm_source: "reply-notifications",
                    utm_medium: "email") %>

Best wishes,

PlanningAlerts
