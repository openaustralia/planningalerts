.comment
  .text= simple_format(comment.text)
  %span.author comment by #{comment.name} #{time_ago_in_words(comment.updated_at)} ago
  - unless current_page?(new_comment_report_path(comment))
    %span.report= link_to "report comment", new_comment_report_path(comment)
    