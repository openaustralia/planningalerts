<%# TODO: Make this use pa_link_to helper instead %>
<%# TODO: Extract this into a component, so it can be reused without remimplenting the component %>
<% if path_to_prev_page(applications) %>
  <%= link_to "< Previous page", path_to_prev_page(applications), class: "text-fuchsia text-2xl font-bold" %>
<% end %>
