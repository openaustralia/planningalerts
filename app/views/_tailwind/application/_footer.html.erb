<div class="my-32">
  <%= render Tailwind::ContainerComponent.new do %>
    <% if Flipper.enabled?(:switch_themes, current_user) %>
      <%= button_to "Toggle theme", toggle_theme_path, class: "text-navy hover:text-white border border-navy hover:bg-navy focus:ring-4 focus:outline-none focus:ring-navy font-medium rounded-lg text-sm px-5 py-2.5 text-center mr-2 mb-2" %>
    <% end %>
  <% end %>
</div>
