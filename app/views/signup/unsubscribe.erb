    <div class="attention">
        <h3>You have been unsubscribed</h3>
        <p>
            You will no longer receive email alerts for any planning applications we find near <strong><%= @user.address %></strong> (within approximately <%= meters_in_words(@user.area_size_meters) %>).
        </p>
        <p>
            If you have unsubscribed because you were getting too many emails, <a href="/">you could always create a new alert for a smaller area?</a>
        </p>
    </div>

