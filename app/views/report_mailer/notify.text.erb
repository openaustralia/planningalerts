The abuse report was completed by <%= @report.name %> (<%= @report.email %>) who said:
<%= @report.details %>

The original comment was written by <%= @comment.name %> who said:
<%= @comment.text %>

The original comment can be found at:
<%= application_url(@comment.application, anchor: "comment#{@comment.id}") %>

To edit/hide/delete this comment:
<%= edit_admin_comment_url(@comment) %>
