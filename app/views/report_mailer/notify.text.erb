The abuse report was completed by <%= @report.name %> (<%= @report.email %>) who said:
<%= @report.details %>

The original comment was written by <%= @report.comment.name %> who said:
<%= @report.comment.text %>

The original comment can be found at:
<%= application_url(@report.comment.application, anchor: "comment#{@report.comment.id}") %>

To edit/hide/delete this comment:
<%= edit_admin_comment_url(@report.comment) %>
