<%= @comment.text.html_safe %>

From <%= @comment.name %> to <%= @comment.councillor.prefixed_name %>

=========================================================================

<%= @comment.name %> posted this message to you on PlanningAlerts in response to the following planning application.

Your reply, and any other response to this email, will be sent to <%= @comment.name %> and posted on the PlanningAlerts website publicly.

Planning Application for <%= @comment.application.address %>

Description: <%= @comment.application.description %>

Read more and see what others have to say here:
<%= application_url(@comment.application,
                    protocol: "https",
                    utm_source: "councillor-notifications",
                    utm_medium: "email",
                    utm_campaign: "view-application") %>

Best wishes,

PlanningAlerts
