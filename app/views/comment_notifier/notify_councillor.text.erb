Hi <%= @comment.councillor.name %>,

You’ve got a new message from <%= @comment.name %> in relation to a local
planning application:

Address: <%= @comment.application.address %>
Description: <%= @comment.application.description %>
Link: <%= application_url(@comment.application, protocol: "https") %>

<%= @comment.name %> writes:

<%= @comment.text.html_safe %>

=========================================================================

This message was posted publicly on PlanningAlerts. You can publicly
respond to this message by simply replying to this email.

If this comment is spam, abusive, unlawful or harassing you can report
it for removal from PlanningAlerts:

<%= new_comment_report_url(@comment, protocol: "https") %>

Best wishes,

PlanningAlerts
