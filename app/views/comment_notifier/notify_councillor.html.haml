%p Hi #{@comment.councillor.name},

%p
  You’ve got a new message from
  = @comment.name
  in relation to a local planning application for #{@comment.application.address}.
  The application is for, “#{@comment.application.description}”.

%p
  = @comment.name
  writes:

%blockquote= comment_as_html(@comment.text)

%p
  This message was posted publicly on PlanningAlerts.
  You can publicly respond to this message by simply replying to this email.

%p If this comment is spam, abusive, unlawful or harassing you can report it for removal from PlanningAlerts.

%p Best wishes,

%p PlanningAlerts
