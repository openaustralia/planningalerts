An abuse report has been filled out for the comment:
<%= @comment.text %>

Author: <%= @comment.name%> (<%= @comment.email %>)
Reported by: <%= @report.name %> (<%= @report.email %>)

The original comment can be found at:
<%= application_url(@comment.application, :anchor => "comment#{@comment.id}") %>

To edit/hide/delete this comment:
<%= rails_admin_edit_url(:model_name => "comments", :id => @comment.id) %>
