The following new planning applications have been found within <%= meters_in_words(alert.radius_meters.to_f) %> of <%= alert.address %>:
<%= render partial: "application", collection: applications[0..14], spacer_template: "spacer", locals: { alert: alert } -%>
<% if applications.size > 15 %>
But wait, there are more! Browse the full list of recent nearby applications:
<%= address_applications_url(q: alert.address) %>

<% end %>
