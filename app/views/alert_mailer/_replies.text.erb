<% if replies.count == 1 %>
The following new reply by a local councillor has been posted on comments within <%= meters_in_words(alert.radius_meters.to_f) %> of <%= alert.address %>:
<% else %>
The following new replies by local councillors have been posted on comments within <%= meters_in_words(alert.radius_meters.to_f) %> of <%= alert.address %>:
<% end %>
<%= render partial: "reply", collection: replies, spacer_template: "spacer" -%>
