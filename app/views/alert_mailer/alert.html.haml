!!!
%html
  %head
    -# rubocop:disable Rails/OutputSafety
    %style= assets_manifest.find_sources("email.css").first.to_s.html_safe
    -# rubocop:enable Rails/OutputSafety

  %body
    = render "header"
    - if @alert.user.requires_activation? && @force_login
      = render "activate_account_call_to_action", alert: @alert
    - unless @comments.empty?
      = render "comments", comments: @comments, alert: @alert
    - unless @applications.empty?
      = render "applications", alert: @alert, applications: @applications
    = render "donation_banner"
    .footer-outer
      .footer-inner
        = render "footer_main"
        = render "footer_actions", alert: @alert, force_login: @force_login
