<%= pa_link_to "< Back to application", @application, extra_classes: "text-xl" %>

<%= render CentreBlockWithImageComponent.new(extra_classes: "mt-16") do |c| %>
  <% c.with_image do %>
    <%= image_tag "tailwind/illustration/send-comment2.svg", alt: "" %>
  <% end %>
  <% c.with_heading do %>
    We will now try to direct you to this application on the <%= @application.authority.full_name %> website
  <% end %>

  <div class="text-xl text-navy">
    <p class="font-bold">
      This may not work!
    </p>
    <p>
      It will depend on how friendly the system is that the planning authority uses.
      If we can't take you directly to the application page we'll take you to a nearby page instead.
    </p>
    <p class="mt-8">
      You'll probably need the application reference number:
    </p>
  </div>

  <div class="mt-2">
    <%= render ClickToCopyComponent.new.with_content(@application.council_reference) %>
  </div>

  <div class="flex flex-col items-center gap-4 mt-12">
    <%= render ButtonComponent.new(tag: :a, size: "xl", type: :primary, href: @application.info_url, open_in_new_tab: true, icon: :external) do %>
      Continue
    <% end %>
    <div class="text-xl text-navy">to <%= @application.authority.full_name %> website</div>
  </div>
<% end %>
