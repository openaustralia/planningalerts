<%= render Tailwind::SelectLinksComponent.new(
      id: "sort",
      label: "Sorted by",
      container: [["most recent", url_for({ sort: "time" }.merge(extra_params))], ["closest", url_for({ sort: "distance" }.merge(extra_params))]],
      value: url_for({ sort: }.merge(extra_params))
    ) %>
