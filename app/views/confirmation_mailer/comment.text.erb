Please click on the link below to confirm your comment. Only then will your comment be visible to others and be sent off to <%= @comment.recipient_display_name %>. If you didn't make a comment, then simply ignore this email and nothing further will happen.

<%= confirmed_comment_url(id: @comment.confirm_id) %>

If your email program does not let you click on this link, just copy and paste it into your web browser and hit return.

Your comment:
<%= @comment.text %>

On the application:
<%= @comment.application.address %> (<%= @comment.application.council_reference %>)

<%= @comment.application.description %>
