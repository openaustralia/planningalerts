The following new planning applications have been found near <%= @alert.address %>:

<%= render :partial => "application", :collection => @applications, :spacer_template => "spacer" -%>
============================================================

PlanningAlerts.org.au is a free service run by the charity OpenAustralia Foundation.

You can subscribe to a geoRSS feed of applications for <%= @alert.address %> here: <%= @georss_url %>

GeoRSS can be used to display planning applications on a map. For example, on Google Maps: http://maps.google.com.au/maps?q=<%= CGI.escape(@georss_url) %>

To stop receiving these emails click here: <%= @unsubscribe_url %>
