<% if @alert.expired_subscription? %>
Your trial subscription has expired
<% else %>
Your trial subscription has <%= pluralize(@alert.subscription.trial_days_remaining, "day") %> remaining
<% end %>
Use this link to subscribe now:
<%= new_subscription_url(
  analytics_params(utm_campaign: @alert.expired_subscription? ? "subscribe-from-expired" : "subscribe-from-trial")
  .merge(utm_content: "text_format_link", email: @alert.email)
)%>
