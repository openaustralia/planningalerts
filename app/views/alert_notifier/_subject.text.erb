<% if !applications.empty? && comments.empty? %>
<%= pluralize(applications.size, "new planning application") %> near <%= alert.address %>
<% elsif applications.empty? && !comments.empty? %>
<%= pluralize(comments.size, "new comment") %> on planning applications near <%= alert.address %>
<% elsif !applications.empty? && !comments.empty? %>
<%= pluralize(comments.size, "new comment") %> and <%= pluralize(applications.size, "new planning application") %> near <%= alert.address %>
<% end %>