
<%= capitalise_initial_character(reply.councillor.prefixed_name) %> replied to <%= reply.comment.name %>’s comment about <%= display_description_with_address(reply.comment.application) %>:

<%= Sanitize.clean(truncate(reply.text, :length => 700), Sanitize::Config::BASIC).html_safe %>

View reply on PlanningAlerts:
<%= reply_url_with_tracking(reply: reply).html_safe %>
