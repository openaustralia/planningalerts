
<%= comment.name %> <%= comment.councillor ? "wrote to #{comment.councillor.prefixed_name}" : "commented on"  %> <%= display_description_with_address(comment.application) %>:

<%= Sanitize.clean(truncate(comment.text, :length => 700), Sanitize::Config::BASIC).html_safe %>

View comment on PlanningAlerts:
<%= comment_url_with_tracking(comment: comment, theme: @theme).html_safe %>
