
<%= Sanitize.clean(truncate(comment.text, :length => 700), Sanitize::Config::BASIC).html_safe %>
by <%= comment.name %> on <%= comment.application.address %>

<%= application_url(comment.application, protocol: @protocol, host: @host, anchor: "comment#{comment.id}", utm_medium: "email", utm_source: "alerts", utm_campaign: "view-comment").html_safe %>
