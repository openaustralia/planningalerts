<%= truncate(Sanitize.clean(comment.text, Sanitize::Config::BASIC), :length => 700).html_safe %>
by <%= comment.name %> on <%= comment.application.address %>

<%= application_url(comment.application, :anchor => "comment#{comment.id}", :utm_medium => "email", :utm_source => "alerts").html_safe %>

