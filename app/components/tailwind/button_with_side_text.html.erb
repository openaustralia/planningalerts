<div class="flex flex-col items-baseline gap-2 md:flex-row md:gap-4">
  <div class="shrink-0">
    <%= render @button do %>
      <%= content %>
    <% end %>
  </div>
  <div class="<%= @button.text_size_class %> text-navy">
    <%= side_text %>
  </div>
</div>
