<% if @tag == :a %>
  <a href="<%= @href %>" class="inline-block px-4 py-2 font-semibold text-white bg-navy">
    <%= content %>
  </a>
<% elsif @tag == :button %>
  <%= button_tag content, class: "px-4 py-2 font-semibold text-white bg-navy" %>
<% end %>