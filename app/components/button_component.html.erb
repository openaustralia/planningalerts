<%= wrapper_tag do %>
  <div class="flex items-baseline justify-center gap-4">
    <% if [:trash, :edit, :external].include?(@icon) %>
      <div class="size-5"><%= render IconComponent.new(name: @icon) %></div>
    <% elsif @icon %>
      <div><%= render IconComponent.new(name: @icon) %></div>
    <% end %>
    <div><%= content %></div>
  </div>
<% end %>
